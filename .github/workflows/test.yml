name: test

on:
  push:
    branches:
      - master
  pull_request: {}

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node-version: [22.x, 24.x, 25.x]
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - run: corepack enable
      - run: yarn install --immutable
      - run: yarn build

  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: latest
      - run: corepack enable
      - run: yarn install --immutable
      - run: yarn build
      - name: Check TypeScript
        run: yarn tsc --noEmit
      - name: Run ESLint
        run: yarn eslint src --ext .ts || true

  test:
    name: Test ${{ matrix.scheme }}
    runs-on: ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        node-version: [22.x, 24.x, 25.x]
        scheme: ['BBS', 'BBS+', 'PS', 'BBDT16']
    env:
      TEST_SIGNATURE_SCHEME: ${{ matrix.scheme }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - run: corepack enable
      - run: yarn install --immutable
      - run: yarn build
      - run: yarn test
